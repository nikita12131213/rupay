с_путем = "examples/data.txt"
попытаться:
    с_файлом = открыть(с_путем, "r", encoding="utf-8")
    содержимое = с_файлом.read()
    печать("Содержимое файла:")
    печать(содержимое)
    с_файлом.close()
кроме FileNotFoundError:
    печать("Файл не найден:", с_путем)
